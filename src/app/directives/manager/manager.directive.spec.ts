import { ManagerDirective } from './manager.directive';

describe('ManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new ManagerDirective();
    expect(directive).toBeTruthy();
  });
});
